# Mini Server WEB

# Test in instance with buybox
# Create new file with extension HTML for user.data
# $ nohup busybox httpd -f -p 8080 &
# Acess in brownser with IP_public:PORT


#Ansible 
# $ ansible-playbook playbook.yml -u ubuntu --private-key PreparandoMaquinasAWScomAnsibleTerraform.pem -i hosts.yml

# -u USUARIO
# --private-key 
# -i hosts
# -vvv visualizar mais detalhes

